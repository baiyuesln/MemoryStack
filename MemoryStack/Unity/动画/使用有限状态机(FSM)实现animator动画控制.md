
#FSM #Animator

### 定义

顾名思义哈,FSM可以理解为 控制 对象 有限状态 的 一种代码表现形式,animator就是一种实现.
	有限状态机（Finite State Machine，FSM）是一种用于建模对象在其生命周期内所经历的状态序列，以及如何响应来自外界的各种事件的工具。FSM具有有限数量的状态，每个状态都有一组进入、退出和更新的行为。当满足特定条件时，FSM会从当前状态转移到另一个状态

优势:

	提高代码的可读性和可维护性，还可以方便地扩展新的状态和行为。

应用场景:

	 玩家动作控制,怪物ai

实现:
